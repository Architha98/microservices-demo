server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:inventorydb
    username: sa
    password: root
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true